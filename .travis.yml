sudo: false

language: node_js

node_js:
  - '10'
  - '8'

cache:
  directories:
    - node_modules

install:
  - yarn

after_script:
  - yarn test:ci
  - yarn coveralls

branches:
  except:
    - /^v[0-9]/
